/** Injection tokens for values loaded at boot (kept apart from the module so importing is cheap). */
export const CONTRACTS = Symbol('CONTRACTS');
export const POLICY_MARKDOWN = Symbol('POLICY_MARKDOWN');
